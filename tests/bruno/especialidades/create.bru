meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/especialidades
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_admin}}
}

body:json {
  {
    "nombre": "DERMATOLOGÍA {{$timestamp}}"
  }
}

settings {
  encodeUrl: true
}
